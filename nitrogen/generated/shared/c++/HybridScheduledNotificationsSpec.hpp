///
/// HybridScheduledNotificationsSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ScheduleFrequency` to properly resolve imports.
namespace margelo::nitro::schedulednotifications { enum class ScheduleFrequency; }
// Forward declaration of `ScheduleDateBase` to properly resolve imports.
namespace margelo::nitro::schedulednotifications { struct ScheduleDateBase; }

#include <string>
#include "ScheduleFrequency.hpp"
#include "ScheduleDateBase.hpp"
#include <functional>

namespace margelo::nitro::schedulednotifications {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ScheduledNotifications`
   * Inherit this class to create instances of `HybridScheduledNotificationsSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridScheduledNotifications: public HybridScheduledNotificationsSpec {
   * public:
   *   HybridScheduledNotifications(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridScheduledNotificationsSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridScheduledNotificationsSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridScheduledNotificationsSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void schedule(const std::string& id, const std::string& title, const std::string& message, ScheduleFrequency frequency, const ScheduleDateBase& scheduleDate) = 0;
      virtual std::string addListener(const std::function<void(const std::string& /* notificationId */)>& callback) = 0;
      virtual void removeListener(const std::string& id) = 0;
      virtual void cancel(const std::string& id) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ScheduledNotifications";
  };

} // namespace margelo::nitro::schedulednotifications
